# Slurm Accounting Configuration (Cluster-Lab)
# Note: AccountingStorageType is disabled (none) — this config
# defines the accounting structure for documentation/testing only.
# To enable full accounting, rebuild with MariaDB and set:
#   AccountingStorageType=accounting_storage/slurmdbd
#   DbdAddr=slurmctl
#   DbdPort=6819
#
# Account-tree mirror for setup_users.sh reference:
#   cluster-lab (parent)
#     ├── alice   (QoS: high)
#     ├── bob     (QoS: normal)
#     └── charlie (QoS: low)
