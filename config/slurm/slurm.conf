# Cluster-Lab Slurm Configuration
# Generated by agent-yuan for zircon / cluster-lab project

# ── 集群名称 ──
ClusterName=cluster-lab

# ── 控制节点 ──
SlurmctldHost=slurmctl(172.20.0.2)

# ── 认证与安全 ──
AuthType=auth/munge
CryptoType=crypto/munge

# ── 调度 ──
SchedulerType=sched/backfill
SchedulerParameters=default_queue_depth=100
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

# ── 通信 ──
# slurmctld 端口
SlurmctldPort=6817
# slurmd 端口
SlurmdPort=6818
# slurmdbd 端口
AccountingStoragePort=6819
# 超时
SlurmdTimeout=60
SlurmctldTimeout=120
InactiveLimit=0
MinJobAge=300
KillWait=30
Waittime=0

# ── 日志 ──
SlurmctldDebug=debug
SlurmctldLogFile=/var/log/slurm/slurmctld.log
SlurmdDebug=debug
SlurmdLogFile=/var/log/slurm/slurmd.log
SlurmdbdDebug=debug
SlurmdbdLogFile=/var/log/slurm/slurmdbd.log
DebugFlags=Steps

# ── 数据库（会计） ──
AccountingStorageType=accounting_storage/slurmdbd
AccountingStorageHost=localhost
AccountingStoragePort=6819
AccountingStorageUser=slurm
AccountingStoragePass=slurm
AccountingStorageDB=slurm_acct_db

# ── 作业记账 ──
JobAcctGatherType=jobacct_gather/cgroup
JobAcctGatherFrequency=30

# ── 节能 ──
PowerSlurmdbdOff=yes

# ── 节点定义 ──
# 管理节点（不用于计算，但有 slurmd 以保持注册）
NodeName=slurmctl CPUs=1 RealMemory=1024 Sockets=1 CoresPerSocket=1 ThreadsPerCore=1 State=UNKNOWN

# 计算节点
NodeName=node01 CPUs=2 RealMemory=2048 Sockets=1 CoresPerSocket=2 ThreadsPerCore=1 State=UNKNOWN
NodeName=node02 CPUs=2 RealMemory=2048 Sockets=1 CoresPerSocket=2 ThreadsPerCore=1 State=UNKNOWN

# ── 分区定义 ──
# 默认分区，包含所有计算节点
PartitionName=normal Nodes=node01,node02 Default=YES MaxTime=INFINITE State=UP

# 管理分区（仅管理节点，用于管理作业）
PartitionName=admin Nodes=slurmctl Default=NO MaxTime=INFINITE State=UP

# 专用分区
PartitionName=debug Nodes=node01 MaxTime=00:30:00 Default=NO State=UP

# ── 任务插件 ──
TaskPlugin=task/cgroup
ProctrackType=proctrack/cgroup

# ── 拓扑 ──
TopologyPlugin=topology/tree
TreeWidth=65533

# ── 返回码 ──
ReturnToService=2
