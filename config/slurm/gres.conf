# Slurm GRES (Generic Resource Scheduling) Configuration
# Simulates GPU devices for job scheduling testing (dev only)

# slurmctl - controller node (no GPU compute)
NodeName=slurmctl Name=gpu Count=0

# node01 - 4 simulated GPUs
NodeName=node01 Name=gpu Count=4

# node02 - 4 simulated GPUs
NodeName=node02 Name=gpu Count=4
